# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ExWidget' do
  use_frameworks!
  
  pod 'Alamofire'
  pod 'Moya/Combine'
  pod 'Kingfisher'
  target 'MyWidgetExtension' do
  end
end
